//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//

// ignore_for_file: unused_element
import 'package:json_annotation/json_annotation.dart';

/// The base item kind.
enum BaseItemKind {
  /// The base item kind.
  @JsonValue(r'AggregateFolder')
  aggregateFolder(r'AggregateFolder'),

  /// The base item kind.
  @JsonValue(r'Audio')
  audio(r'Audio'),

  /// The base item kind.
  @JsonValue(r'AudioBook')
  audioBook(r'AudioBook'),

  /// The base item kind.
  @JsonValue(r'BasePluginFolder')
  basePluginFolder(r'BasePluginFolder'),

  /// The base item kind.
  @JsonValue(r'Book')
  book(r'Book'),

  /// The base item kind.
  @JsonValue(r'BoxSet')
  boxSet(r'BoxSet'),

  /// The base item kind.
  @JsonValue(r'Channel')
  channel(r'Channel'),

  /// The base item kind.
  @JsonValue(r'ChannelFolderItem')
  channelFolderItem(r'ChannelFolderItem'),

  /// The base item kind.
  @JsonValue(r'CollectionFolder')
  collectionFolder(r'CollectionFolder'),

  /// The base item kind.
  @JsonValue(r'Episode')
  episode(r'Episode'),

  /// The base item kind.
  @JsonValue(r'Folder')
  folder(r'Folder'),

  /// The base item kind.
  @JsonValue(r'Genre')
  genre(r'Genre'),

  /// The base item kind.
  @JsonValue(r'ManualPlaylistsFolder')
  manualPlaylistsFolder(r'ManualPlaylistsFolder'),

  /// The base item kind.
  @JsonValue(r'Movie')
  movie(r'Movie'),

  /// The base item kind.
  @JsonValue(r'LiveTvChannel')
  liveTvChannel(r'LiveTvChannel'),

  /// The base item kind.
  @JsonValue(r'LiveTvProgram')
  liveTvProgram(r'LiveTvProgram'),

  /// The base item kind.
  @JsonValue(r'MusicAlbum')
  musicAlbum(r'MusicAlbum'),

  /// The base item kind.
  @JsonValue(r'MusicArtist')
  musicArtist(r'MusicArtist'),

  /// The base item kind.
  @JsonValue(r'MusicGenre')
  musicGenre(r'MusicGenre'),

  /// The base item kind.
  @JsonValue(r'MusicVideo')
  musicVideo(r'MusicVideo'),

  /// The base item kind.
  @JsonValue(r'Person')
  person(r'Person'),

  /// The base item kind.
  @JsonValue(r'Photo')
  photo(r'Photo'),

  /// The base item kind.
  @JsonValue(r'PhotoAlbum')
  photoAlbum(r'PhotoAlbum'),

  /// The base item kind.
  @JsonValue(r'Playlist')
  playlist(r'Playlist'),

  /// The base item kind.
  @JsonValue(r'PlaylistsFolder')
  playlistsFolder(r'PlaylistsFolder'),

  /// The base item kind.
  @JsonValue(r'Program')
  program(r'Program'),

  /// The base item kind.
  @JsonValue(r'Recording')
  recording(r'Recording'),

  /// The base item kind.
  @JsonValue(r'Season')
  season(r'Season'),

  /// The base item kind.
  @JsonValue(r'Series')
  series(r'Series'),

  /// The base item kind.
  @JsonValue(r'Studio')
  studio(r'Studio'),

  /// The base item kind.
  @JsonValue(r'Trailer')
  trailer(r'Trailer'),

  /// The base item kind.
  @JsonValue(r'TvChannel')
  tvChannel(r'TvChannel'),

  /// The base item kind.
  @JsonValue(r'TvProgram')
  tvProgram(r'TvProgram'),

  /// The base item kind.
  @JsonValue(r'UserRootFolder')
  userRootFolder(r'UserRootFolder'),

  /// The base item kind.
  @JsonValue(r'UserView')
  userView(r'UserView'),

  /// The base item kind.
  @JsonValue(r'Video')
  video(r'Video'),

  /// The base item kind.
  @JsonValue(r'Year')
  year(r'Year');

  const BaseItemKind(this.value);

  final String value;

  @override
  String toString() => value;
}
