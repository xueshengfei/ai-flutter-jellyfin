//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//

// ignore_for_file: unused_element
import 'package:json_annotation/json_annotation.dart';

/// These represent sort orders.
enum ItemSortBy {
  /// These represent sort orders.
  @JsonValue(r'Default')
  default_(r'Default'),

  /// These represent sort orders.
  @JsonValue(r'AiredEpisodeOrder')
  airedEpisodeOrder(r'AiredEpisodeOrder'),

  /// These represent sort orders.
  @JsonValue(r'Album')
  album(r'Album'),

  /// These represent sort orders.
  @JsonValue(r'AlbumArtist')
  albumArtist(r'AlbumArtist'),

  /// These represent sort orders.
  @JsonValue(r'Artist')
  artist(r'Artist'),

  /// These represent sort orders.
  @JsonValue(r'DateCreated')
  dateCreated(r'DateCreated'),

  /// These represent sort orders.
  @JsonValue(r'OfficialRating')
  officialRating(r'OfficialRating'),

  /// These represent sort orders.
  @JsonValue(r'DatePlayed')
  datePlayed(r'DatePlayed'),

  /// These represent sort orders.
  @JsonValue(r'PremiereDate')
  premiereDate(r'PremiereDate'),

  /// These represent sort orders.
  @JsonValue(r'StartDate')
  startDate(r'StartDate'),

  /// These represent sort orders.
  @JsonValue(r'SortName')
  sortName(r'SortName'),

  /// These represent sort orders.
  @JsonValue(r'Name')
  name(r'Name'),

  /// These represent sort orders.
  @JsonValue(r'Random')
  random(r'Random'),

  /// These represent sort orders.
  @JsonValue(r'Runtime')
  runtime(r'Runtime'),

  /// These represent sort orders.
  @JsonValue(r'CommunityRating')
  communityRating(r'CommunityRating'),

  /// These represent sort orders.
  @JsonValue(r'ProductionYear')
  productionYear(r'ProductionYear'),

  /// These represent sort orders.
  @JsonValue(r'PlayCount')
  playCount(r'PlayCount'),

  /// These represent sort orders.
  @JsonValue(r'CriticRating')
  criticRating(r'CriticRating'),

  /// These represent sort orders.
  @JsonValue(r'IsFolder')
  isFolder(r'IsFolder'),

  /// These represent sort orders.
  @JsonValue(r'IsUnplayed')
  isUnplayed(r'IsUnplayed'),

  /// These represent sort orders.
  @JsonValue(r'IsPlayed')
  isPlayed(r'IsPlayed'),

  /// These represent sort orders.
  @JsonValue(r'SeriesSortName')
  seriesSortName(r'SeriesSortName'),

  /// These represent sort orders.
  @JsonValue(r'VideoBitRate')
  videoBitRate(r'VideoBitRate'),

  /// These represent sort orders.
  @JsonValue(r'AirTime')
  airTime(r'AirTime'),

  /// These represent sort orders.
  @JsonValue(r'Studio')
  studio(r'Studio'),

  /// These represent sort orders.
  @JsonValue(r'IsFavoriteOrLiked')
  isFavoriteOrLiked(r'IsFavoriteOrLiked'),

  /// These represent sort orders.
  @JsonValue(r'DateLastContentAdded')
  dateLastContentAdded(r'DateLastContentAdded'),

  /// These represent sort orders.
  @JsonValue(r'SeriesDatePlayed')
  seriesDatePlayed(r'SeriesDatePlayed'),

  /// These represent sort orders.
  @JsonValue(r'ParentIndexNumber')
  parentIndexNumber(r'ParentIndexNumber'),

  /// These represent sort orders.
  @JsonValue(r'IndexNumber')
  indexNumber(r'IndexNumber');

  const ItemSortBy(this.value);

  final String value;

  @override
  String toString() => value;
}
