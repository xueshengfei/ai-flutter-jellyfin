//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//

// ignore_for_file: unused_element
import 'package:json_annotation/json_annotation.dart';

/// Used to control the data that gets attached to DtoBaseItems.
enum ItemFields {
  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'AirTime')
  airTime(r'AirTime'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'CanDelete')
  canDelete(r'CanDelete'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'CanDownload')
  canDownload(r'CanDownload'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ChannelInfo')
  channelInfo(r'ChannelInfo'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Chapters')
  chapters(r'Chapters'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Trickplay')
  trickplay(r'Trickplay'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ChildCount')
  childCount(r'ChildCount'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'CumulativeRunTimeTicks')
  cumulativeRunTimeTicks(r'CumulativeRunTimeTicks'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'CustomRating')
  customRating(r'CustomRating'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'DateCreated')
  dateCreated(r'DateCreated'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'DateLastMediaAdded')
  dateLastMediaAdded(r'DateLastMediaAdded'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'DisplayPreferencesId')
  displayPreferencesId(r'DisplayPreferencesId'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Etag')
  etag(r'Etag'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ExternalUrls')
  externalUrls(r'ExternalUrls'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Genres')
  genres(r'Genres'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ItemCounts')
  itemCounts(r'ItemCounts'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'MediaSourceCount')
  mediaSourceCount(r'MediaSourceCount'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'MediaSources')
  mediaSources(r'MediaSources'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'OriginalTitle')
  originalTitle(r'OriginalTitle'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Overview')
  overview(r'Overview'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ParentId')
  parentId(r'ParentId'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Path')
  path(r'Path'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'People')
  people(r'People'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'PlayAccess')
  playAccess(r'PlayAccess'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ProductionLocations')
  productionLocations(r'ProductionLocations'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ProviderIds')
  providerIds(r'ProviderIds'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'PrimaryImageAspectRatio')
  primaryImageAspectRatio(r'PrimaryImageAspectRatio'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'RecursiveItemCount')
  recursiveItemCount(r'RecursiveItemCount'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Settings')
  settings(r'Settings'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'SeriesStudio')
  seriesStudio(r'SeriesStudio'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'SortName')
  sortName(r'SortName'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'SpecialEpisodeNumbers')
  specialEpisodeNumbers(r'SpecialEpisodeNumbers'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Studios')
  studios(r'Studios'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Taglines')
  taglines(r'Taglines'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Tags')
  tags(r'Tags'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'RemoteTrailers')
  remoteTrailers(r'RemoteTrailers'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'MediaStreams')
  mediaStreams(r'MediaStreams'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'SeasonUserData')
  seasonUserData(r'SeasonUserData'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'DateLastRefreshed')
  dateLastRefreshed(r'DateLastRefreshed'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'DateLastSaved')
  dateLastSaved(r'DateLastSaved'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'RefreshState')
  refreshState(r'RefreshState'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ChannelImage')
  channelImage(r'ChannelImage'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'EnableMediaSourceDisplay')
  enableMediaSourceDisplay(r'EnableMediaSourceDisplay'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Width')
  width(r'Width'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'Height')
  height(r'Height'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'ExtraIds')
  extraIds(r'ExtraIds'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'LocalTrailerCount')
  localTrailerCount(r'LocalTrailerCount'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'IsHD')
  isHD(r'IsHD'),

  /// Used to control the data that gets attached to DtoBaseItems.
  @JsonValue(r'SpecialFeatureCount')
  specialFeatureCount(r'SpecialFeatureCount');

  const ItemFields(this.value);

  final String value;

  @override
  String toString() => value;
}
