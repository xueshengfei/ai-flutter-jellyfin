name: jellyfin_models
description: Jellyfin 共享业务模型 - 跨模块共用的纯 Dart 模型
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  equatable: ^2.0.7

dev_dependencies:
  test: ^1.25.0
