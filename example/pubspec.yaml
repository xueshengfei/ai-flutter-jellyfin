name: jellyfin_service_example
description: Jellyfin Service 测试应用
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 引用父目录的 jellyfin_service
  jellyfin_service:
    path: ../

  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.2
  video_player: ^2.8.1
  chewie: ^1.7.5
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
